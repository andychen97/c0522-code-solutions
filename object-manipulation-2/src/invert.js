/* exported invert */

function invert(source) {
  var obj = {};

  for (var i = 0; i < source.length; i++) {
    var key = source[i];
    var value = source[i[i]];
    obj[key] = value;
  }
  return obj;
}
