/* exported setValue */

/*
- define function with 3 parameters (object, key, value)
- formulate equation to choose an object and add a key:value pair
*/

function setValue(object, key, value) {
  object[key] = value;
}
