/* exported firstChars */

/*
- define a function with 2 parameters (length, string)
- use the slice method with 2 arguments of 0, length
- return string
*/

function firstChars(length, string) {
  return string.slice(0, length);
}
