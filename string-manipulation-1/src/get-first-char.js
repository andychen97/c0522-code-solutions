/* exported getFirstChar */

/*
- define a function with 1 parameter of string
- use the charAt method to know the first character of the string
- return character
*/

function getFirstChar(string) {
  return string.charAt(0);
}
